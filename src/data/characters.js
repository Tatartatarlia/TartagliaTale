/**
 * characters
 * - 存放角色基础信息，用于剧情内页展示
 * - 常见字段：
 *   - id / name
 *   - portraitUrl / 立绘资源路径（后续你可以接入 src/assets/images）
 *   - 默认情绪样式或主题色
 *
 * 与 UI 的关系：
 * - CharacterDisplay 组件通过 speakerId 查找角色信息并展示。
 */
