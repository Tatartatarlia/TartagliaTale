/**
 * useGame
 * - 封装“剧情推进”的核心业务逻辑（选择选项、推进到下一个节点、计算好感度变化、触发失败等）
 * - 封装“失败后返回/重新选择”的逻辑：回滚到 choice 发生前的 checkpoint（nodeId + affection + history）
 * - 与唯一存档策略配合：每次关键状态变化后可触发保存（或由上层统一触发）
 *
 * 你当前决定：只有一个存档且失败重选是“新分支”，所以这里会重点处理：
 * - checkpoint（选择发生前的快照）
 * - state 覆盖（覆盖写入唯一存档，而不是追加时间线）
 */
