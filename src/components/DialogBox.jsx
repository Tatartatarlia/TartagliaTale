/**
 * DialogBox
 * - 剧情内页“对话框”的展示组件
 * - 显示当前 node 的文本内容（可支持分段逐字显示，后续再加）
 * - 展示当前可选项列表（可选：也可以把选项渲染拆成 ChoiceList 独立组件）
 * - 对外暴露事件：当用户点击某个选项时调用 onChoice(choiceId)
 *
 你现在的功能重点：
 * - 错误选项会触发失败并进入失败页
 * - 失败页允许返回 choice 发生前并重新选择
 */
